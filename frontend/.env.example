# Leave empty for local `npm run dev` (Vite proxies /api → http://127.0.0.1:8000).
# On Vercel, set this to the public API origin (no trailing slash):
#   VITE_API_BASE_URL=https://clinical-trial-navigator-api.fly.dev
VITE_API_BASE_URL=
